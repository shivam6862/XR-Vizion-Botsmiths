import React from "react";
import classes from "@/styles/account/reset.module.css";

const Reset = () => {
  return (
    <div className={classes.container}>
      <div className={classes.box}>Reset password</div>
    </div>
  );
};

export default Reset;
