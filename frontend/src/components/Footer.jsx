import React from "react";
import classes from "@/styles/footer.module.css";

const Footer = () => {
  return (
    <div className={classes.container}>
      <div className={classes.box}>Footer</div>
    </div>
  );
};

export default Footer;
